name: Build Android APK
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libsqlite3-dev openjdk-17-jdk python3-pip zip unzip
          pip install --upgrade buildozer cython==0.29.33

      # СЕРВЕР САМ СКАЧАЕТ ВЕСЬ ЧИСТЫЙ КОД ИДЕАЛЬНО И БЕЗ ОБРЕЗАНИЙ
      - name: Download Full Main Code
        run: |
          wget -O main.py https://pastebin.com

      - name: Build APK with Buildozer
        run: |
          yes y | buildozer -v android debug

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: fitness-diary-apk
          path: bin/*.apk
